var escapeRoom = (wordlist, keypads)=> {



  // iterate keypads left to right

    // for each keypad, iterate left to right over wordlist
    // iterate over each letter in each word. If letter exists in keypad, continue to iterate until end of wordlist
    // if end of word is reached, and all letters exist in keypad set counter to 1
    // otherewise, if letter does not exist, continue to next word in wordlist
    // at the end of each word iteration, push counter into result array





  // return result array

  };


  //I array of words and array of keypads in all caps
  // array of ints representing # of words in wordlist spelled from each keypad at that index
  //C
  //E

  escapeRoom(['APPLE', 'PLEAS', 'PLEASE'], ['AELWXYZ', 'AELPXYZ', 'AELPSXY', 'SAELPRT', 'XAEBKSY'])
  // [0, 1, 3, 2, 0]